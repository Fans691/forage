package com.forage.forage.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.forage.forage.model.ConfigStatut;

public interface ConfigStatutRepository extends JpaRepository<ConfigStatut, Long> {
}
